name: Daily Prayer Times

on:
  workflow_dispatch:
    schedule:
      - cron: "30 03 * * *"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Repository check
        uses: actions/checkout@v3

      - name: Daily Prayer Times
        run: echo -ne "# namaz-vakitleri\n\nTürkiye için namaz vakitlerini, Diyanet'in sitesinden günlük ve aylık olarak, belirtilen il için gösteren betik.\n\n" README.md && ./namaz 34 >> README.md 
        shell: bash

      - name: Update README.md
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: 🔼 Prayer times updated automatically
